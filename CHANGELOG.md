# 变更记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-22

首个发布版本。

### 功能

- **设备模板**：可视化编辑识别协议 —— 命令格式（文本 / 十六进制）、命令内容、文本结束符、
  响应格式、预期响应、匹配方式（包含 / 完全相等）、文本编码（ASCII / UTF-8 / GBK）、超时时间。
- **模板管理**：左侧列表支持新增 / 复制 / 删除；每个模板可单独启用或停用。
- **串口参数组合**：内置 26 组常用组合目录（波特率 × 校验位 × 停止位），逐行勾选启用；
  提供「全选」「选择常用的」；行序按「已勾选 → 8 位数据位常用 → 7 位数据位不常用」自动排序，
  行序即扫描顺序。
- **顺序扫描**：端口按 COM 号自然排序（COM10 在 COM2 之后）；端口外层、模板中层、参数内层依次探测；
  任一模板命中即停止该端口的后续探测；扫描前对模板与参数做深拷贝快照，扫描中改界面不影响本轮结果。
- **实时反馈**：匹配结果表、识别到的设备汇总、扫描日志、状态栏摘要四路同步输出。
- **可中断**：点击「停止搜索」会在当前串口安全关闭后结束本轮，并明确标注「部分结果」，
  不会误报为「搜索完成」。
- **校验闭环**：单模板字段校验 + 全部模板重名校验；不通过时禁用「开始搜索」并列出具体原因。
- **配置持久化**：模板保存到程序目录的 `devices.json`（临时文件 + `File.Replace` 原子写入）；
  旧配置缺 `Enabled` 字段时默认启用，向后兼容。

### 已知限制

- 串行探测、不并行：耗时随「端口数 × 模板数 × 参数组合数」线性增长。
- 不自动识别协议：需要使用者先知道设备的查询命令。
