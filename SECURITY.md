# 安全政策

## 重要：本工具会向串口发送数据

「串口设备搜索器」的设计用途是**识别设备接在哪个串口上**。为此，它会把你配置的
**识别命令真实发送到每一个候选串口**。这不是一个只读工具。

因此，使用前请确认：

- 模板中的命令必须是**确认无副作用的查询或状态读取命令**；
- **不要**配置会启动设备、修改参数、清除数据、执行复位或改变生产状态的命令；
- 首次在某个设备上使用，建议先只启用一个模板、只勾选一组串口参数，小范围验证命令无害后再放开。

## 报告安全问题

如果你发现了安全相关的问题（例如：可能被滥用来发送破坏性命令、
配置解析存在风险、越权访问等），请通过以下方式报告：

- 优先使用 GitHub 的[私密漏洞报告](https://github.com/clxa/Serial-Port-Finder/security/advisories/new)功能；
- 或直接在该仓库开一个 Issue（**请勿在公开 Issue 中粘贴任何敏感信息**，如真实设备地址、内网信息、凭据）。

我会尽快确认并回复。请给我合理的修复时间，再公开披露细节。

## 不在范围内

- **误用造成的后果**：把有副作用的命令配进模板并运行，属于使用者配置问题。
- **设备本身的缺陷**：设备固件、驱动、串口线缆引发的问题。
- **第三方依赖的漏洞**：请直接向对应项目报告（本项目使用 Newtonsoft.Json、NUnit、Roslynator 等）。

## 支持的版本

当前仅维护最新发布版本。
